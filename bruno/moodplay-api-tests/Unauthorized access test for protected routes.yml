info:
  name: Unauthorized access test for protected routes
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}/api/profile"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is unauthorized", function () {
          expect([401, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
