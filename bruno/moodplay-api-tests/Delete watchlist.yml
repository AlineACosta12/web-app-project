info:
  name: Delete watchlist
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{baseUrl}}/api/watchlist/550"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
