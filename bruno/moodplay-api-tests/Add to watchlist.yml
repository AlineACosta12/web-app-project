info:
  name: Add to watchlist
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/api/watchlist/550"
  body:
    type: json
    data: |-
      {
        "title": "Fight Club"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
