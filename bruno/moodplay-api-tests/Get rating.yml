info:
  name: Get rating
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}/api/ratings/550"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
