info:
  name: Update rating
  type: http
  seq: 17

http:
  method: PUT
  url: "{{baseUrl}}/api/ratings/550"
  body:
    type: json
    data: |-
      {
        "title": "Fight Club",
        "score": 4,
        "review": "Still great"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
