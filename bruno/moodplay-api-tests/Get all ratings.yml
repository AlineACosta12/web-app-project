info:
  name: Get all ratings
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/api/ratings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
