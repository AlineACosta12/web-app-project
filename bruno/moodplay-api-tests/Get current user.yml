info:
  name: Get current user
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/auth/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
