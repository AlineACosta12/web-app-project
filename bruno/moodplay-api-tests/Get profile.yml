info:
  name: Get profile
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
