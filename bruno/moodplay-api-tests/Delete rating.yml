info:
  name: Delete rating
  type: http
  seq: 18

http:
  method: DELETE
  url: "{{baseUrl}}/api/ratings/550"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
