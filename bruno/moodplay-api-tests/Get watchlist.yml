info:
  name: Get watchlist
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/watchlist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
